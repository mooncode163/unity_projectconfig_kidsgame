// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN
buildscript {
	repositories {
		jcenter()
	}

	dependencies {
	//gradle 版本要和主目录下 .gradle和 gradle文件夹下gradle-wrapper.properties设置的保持一致
	//如2.3.3对应于gradle-wrapper.properties里设置的3.3版本
	//同时apk-build 对应的gradle bin 路径（环境变量）也要指定为对应的版本
		classpath 'com.android.tools.build:gradle:2.3.3'
	}
}

allprojects {
   repositories {
      flatDir {
        dirs 'libs'
      }
   }
}

apply plugin: 'com.android.application'

dependencies {
	compile fileTree(include: ['*.jar'], dir: 'libs')
	compile fileTree(include: ['*.jar'], dir: 'libs/umeng')
	compile fileTree(include: ['*.jar'], dir: 'libs/share')
	compile fileTree(include: ['*.jar'], dir: 'libs/xiaomi_iap')
	compile fileTree(include: ['*.jar'], dir: 'libs/ad_lib')
	compile fileTree(include: ['*.jar'], dir: 'libs/ad_lib/xiaomi')
	compile fileTree(include: ['*.jar'], dir: 'libs/ad_lib/gdt')
	//	compile 'com.android.support:multidex:'
	compile 'com.google.android.gms:play-services:11.0.4'
	compile 'com.android.support:appcompat-v7:26.+'
	compile(name: 'unity-ads', ext: 'aar')
	compile files('libs/share/weiboSDKCore.jar')
	compile files('libs/share/SocialSDK_Sina_Full.jar')
}

android {
	compileSdkVersion 26
	buildToolsVersion '26.0.1'

	defaultConfig {
		targetSdkVersion 26
		multiDexEnabled true
		minSdkVersion 21
	}

	lintOptions {
		abortOnError false
	}


	signingConfigs {
		releaseConfig {
			keyAlias 'moonma'
			keyPassword 'qianlizhiwai'
			storeFile file('./src/main/apk_sign_key.jks')
			storePassword 'qianlizhiwai'
		}
	}

	buildTypes {
		debug {
			jniDebuggable true
		}
		release {
			// Set minifyEnabled to true if you want to run ProGuard on your project
			minifyEnabled false
			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
			signingConfig signingConfigs.releaseConfig
			
		}
	}

}
