// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
}
}

allprojects {
    repositories {
        google()
        jcenter()
        flatDir {
            dirs 'libs'
        }
    }
}

apply plugin: 'com.android.application'


dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

//
    implementation 'com.android.support.constraint:constraint-layout:1.1.3'
    implementation fileTree(include: ['*.jar'], dir: 'libs/umeng')
    implementation fileTree(include: ['*.jar'], dir: 'libs/share')
    implementation fileTree(include: ['*.jar'], dir: 'libs/xiaomi_iap')
    implementation fileTree(include: ['*.jar'], dir: 'libs/ad_lib')
    implementation fileTree(include: ['*.jar'], dir: 'libs/ad_lib/xiaomi')
    implementation fileTree(include: ['*.jar'], dir: 'libs/ad_lib/gdt')
    implementation fileTree(include: ['*.jar'], dir: 'libs/ad_lib/adview')
    implementation fileTree(include: ['*.jar'], dir: 'libs/ad_lib/vungle')
    //	compile 'com.android.support:multidex:'
    implementation 'com.google.android.gms:play-services:11.0.4'
    implementation 'com.android.support:appcompat-v7:28.0.0'
    implementation(name: 'unity-ads', ext: 'aar')
    implementation files('libs/share/weiboSDKCore.jar')
    implementation files('libs/share/SocialSDK_Sina_Full.jar')

    //mobvista
    // implementation(name: 'mintegral_alphab', ext: 'aar')
    // implementation(name: 'mintegral_appwall', ext: 'aar')
    // implementation(name: 'mintegral_appwallext', ext: 'aar')
    // implementation(name: 'mintegral_common', ext: 'aar')
    // implementation(name: 'mintegral_interstitial', ext: 'aar')
    // implementation(name: 'mintegral_interstitialvideo', ext: 'aar')
    // implementation(name: 'mintegral_mtgdownloads', ext: 'aar')
    // implementation(name: 'mintegral_mtgjscommon', ext: 'aar')
    // implementation(name: 'mintegral_mtgnative', ext: 'aar')
    // implementation(name: 'mintegral_nativeex', ext: 'aar')
    // implementation(name: 'mintegral_offerwall', ext: 'aar')
    // implementation(name: 'mintegral_playercommon', ext: 'aar')
    // implementation(name: 'mintegral_reward', ext: 'aar')
    // implementation(name: 'mintegral_videocommon', ext: 'aar')
    // implementation(name: 'mintegral_videofeeds', ext: 'aar')
    // implementation(name: 'mintegral_videojs', ext: 'aar')
    // implementation(name: 'mintegral_optimizedata', ext: 'aar')
    //mobvista

}

android {
    compileSdkVersion 28
    buildToolsVersion '28.0.3'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion 26
        targetSdkVersion 28
       // applicationId 'com.moonma.kidsgame'
        ndk { 
            abiFilters 'armeabi-v7a', 'arm64-v8a'
        }
     //   versionCode 1
     //   versionName '0.1'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress = ['.unity3d', '.ress', '.resource', '.obb'] 
        ignoreAssetsPattern = "!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~"
    }

    signingConfigs {
        release {
            keyAlias 'moonma'
            keyPassword 'qianlizhiwai'
            storeFile file('./src/main/apk_sign_key.jks')
            storePassword 'qianlizhiwai'
			v1SigningEnabled true  
			v2SigningEnabled true 
        }
    }

    buildTypes {
        debug {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.debug
            jniDebuggable true
        }
        release {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.release
        }
    }

    packagingOptions { 
        doNotStrip '*/armeabi-v7a/*.so'
        doNotStrip '*/arm64-v8a/*.so'
    }


    bundle {
        language {
            enableSplit = false
        }
        density {
            enableSplit = false
        }
        abi {
            enableSplit = true
        }
    }
}
